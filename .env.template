# 统一数据目录配置文件
# Unified Data Directory Configuration

# 数据根目录 (可通过环境变量 TRADINGAGENTS_DATA_DIR 覆盖)
TRADINGAGENTS_DATA_DIR=./data

# 缓存目录 (可通过环境变量 TRADINGAGENTS_CACHE_DIR 覆盖)
TRADINGAGENTS_CACHE_DIR=./data/cache

# 分析结果目录 (可通过环境变量 TRADINGAGENTS_RESULTS_DIR 覆盖)
TRADINGAGENTS_RESULTS_DIR=./data/analysis_results

# 会话数据目录 (可通过环境变量 TRADINGAGENTS_SESSIONS_DIR 覆盖)
TRADINGAGENTS_SESSIONS_DIR=./data/sessions

# 日志目录 (可通过环境变量 TRADINGAGENTS_LOGS_DIR 覆盖)
TRADINGAGENTS_LOGS_DIR=./data/logs

# 配置目录 (可通过环境变量 TRADINGAGENTS_CONFIG_DIR 覆盖)
TRADINGAGENTS_CONFIG_DIR=./data/config

# 临时文件目录 (可通过环境变量 TRADINGAGENTS_TEMP_DIR 覆盖)
TRADINGAGENTS_TEMP_DIR=./data/temp

# 数据库目录
TRADINGAGENTS_DB_DIR=./data/databases

# MongoDB 数据目录
MONGODB_DATA_DIR=./data/databases/mongodb

# Redis 数据目录
REDIS_DATA_DIR=./data/databases/redis

# 其他现有配置保持不变...
# API Keys
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
DEEPSEEK_API_KEY=your_deepseek_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here
FINNHUB_API_KEY=your_finnhub_api_key_here
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_api_key_here

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/tradingagents
REDIS_URL=redis://localhost:6379/0

# Web Configuration
WEB_HOST=localhost
WEB_PORT=8000
WEB_DEBUG=true

# Logging Configuration
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Cache Configuration
CACHE_TTL=3600
CACHE_MAX_SIZE=1000

# Analysis Configuration
DEFAULT_ANALYSIS_PERIOD=30
MAX_CONCURRENT_ANALYSIS=5